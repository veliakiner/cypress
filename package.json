{
  "name": "cypress-demo",
  "version": "1.0.0",
  "description": "Write an end-to-end test using <member-of-cupressacae> and <member-of-cucurbitaceae>",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run --reporter junit --reporter-options 'mochaFile=results/my-test-output.xml,toConsole=true'",
    "debug": "npx cypress open",
    "test-no-report": "npx cypress run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^9.1.0",
    "cypress-cucumber-preprocessor": "^4.3.0",
    "mochawesome": "^7.0.1"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": true
  }
}
